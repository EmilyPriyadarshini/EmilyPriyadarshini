name: Generate Dark-Mode Snake

# Run automatically daily or manually
on:
  schedule:
    - cron: "0 0 * * *"   # every 24 hours
  workflow_dispatch:      # allows manual trigger

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Step 1: checkout your repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # Step 2: generate dark-mode snake
      - name: Generate Dark Snake
        uses: Platane/snk@v3.3.0
        with:
          github_user_name: EmilyPriyadarshini
          outputs: dist/snake-dark.svg
          theme: dark

      # Step 3: push generated snake to output branch
      - name: Push Snake to output
